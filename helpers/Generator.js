class Generator {
    generateValues(numberOfPairs) {
        values = [];

        return values;
    }

    getBoardMapping(numberOfPairs) {
        rows = 0;
        cols = 0;

        return {rows: rows, cols: cols}
    }
}